import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#ffff00',
        },
        blue: {
            main: "#4D98FE"
        },
        white: {
            main: "#ffffff"
        }
    },
});

export default theme;
